# Карта системы GRAсклад

## Архитектура — общий вид

```mermaid
graph TB
    subgraph BROWSER["🌐 Браузер"]
        ADMIN["👤 Админ<br/>13 страниц"]
        EMPLOYEE["👷 Сотрудник<br/>6 страниц"]
    end

    subgraph FRONTEND["⚛️ Frontend — React 18 + Vite + Tailwind"]
        ROUTER["React Router<br/>Lazy Loading"]
        COMPONENTS["Компоненты<br/>Modal, Button, Toast, Icons"]
        CONTEXT["Контекст<br/>Auth, Theme, Settings"]
        API_CLIENT["Axios Client<br/>JWT Auto-header"]
    end

    subgraph BACKEND["🟢 Backend — Express + Node.js"]
        ROUTES["14 роутеров<br/>150+ эндпоинтов"]
        AUTH["JWT Auth<br/>Roles & Permissions"]
        MIDDLEWARE["Middleware<br/>Rate Limit, CORS"]
    end

    subgraph DATABASE["🐘 PostgreSQL"]
        TABLES["33 таблицы<br/>Auto-migration"]
        TRIGGERS["Триггеры<br/>updated_at"]
    end

    subgraph EXTERNAL["🔗 Внешние системы"]
        MS["МойСклад API<br/>Импорт каталога"]
        OZON["OZON API<br/>2 магазина"]
        WB["Wildberries API<br/>2 магазина"]
        SBORKA["Сборка-сайт<br/>Заказы OZ/WB"]
    end

    subgraph DEPLOY["🚀 Деплой"]
        GH["GitHub Actions<br/>CI/CD"]
        PM2["PM2<br/>Process Manager"]
        NGINX["Nginx<br/>Reverse Proxy"]
    end

    ADMIN --> ROUTER
    EMPLOYEE --> ROUTER
    ROUTER --> COMPONENTS
    ROUTER --> CONTEXT
    COMPONENTS --> API_CLIENT
    API_CLIENT -->|HTTP/JSON| ROUTES
    ROUTES --> AUTH
    AUTH --> MIDDLEWARE
    ROUTES --> TABLES
    TABLES --> TRIGGERS
    ROUTES -->|HTTPS| MS
    ROUTES -->|HTTPS| OZON
    ROUTES -->|HTTPS| WB
    ROUTES -->|Webhook| SBORKA
    GH -->|SSH| PM2
    PM2 --> BACKEND
    NGINX -->|:3017| BACKEND

    style BROWSER fill:#f5f3ff,stroke:#7c3aed
    style FRONTEND fill:#ede9fe,stroke:#7c3aed
    style BACKEND fill:#d1fae5,stroke:#059669
    style DATABASE fill:#dbeafe,stroke:#2563eb
    style EXTERNAL fill:#fef3c7,stroke:#d97706
    style DEPLOY fill:#fee2e2,stroke:#dc2626
```

## Модули и связи

```mermaid
graph LR
    subgraph PRODUCTS["📦 Товары"]
        CARDS["Карточки"]
        STOCK["Остатки"]
        MATERIALS["Сырьё"]
        BARCODES["Штрих-коды"]
    end

    subgraph WAREHOUSE["🏭 Склады"]
        FBS["Стеллажный<br/>Стеллажи → Полки → Ячейки"]
        FBO["Паллетный<br/>Ряды → Паллеты → Коробки"]
        VISUAL["Визуальный вид<br/>FBS/FBO"]
    end

    subgraph OPERATIONS["⚙️ Операции"]
        TASKS["Задачи<br/>Инвентаризация"]
        PACKING["Упаковка<br/>Фасовка"]
        ASSEMBLY["Сборка комплектов<br/>Assembly"]
        MOVEMENTS["Перемещения<br/>Лог операций"]
    end

    subgraph PEOPLE["👥 Люди"]
        STAFF["Сотрудники"]
        EARNINGS["Заработок<br/>GRACoin"]
        FEEDBACK["Обратная связь"]
    end

    subgraph ANALYTICS["📊 Аналитика"]
        REPORTS["Отчёты"]
        DASHBOARD["Дашборд"]
        ERRORS["Ошибки"]
    end

    subgraph INTEGRATIONS["🔗 Интеграции"]
        MOYSKLAD["МойСклад"]
        OZON_INT["OZON × 2"]
        WB_INT["WB × 2"]
    end

    CARDS --> BARCODES
    CARDS --> MATERIALS
    MATERIALS --> STOCK
    CARDS --> FBS
    CARDS --> FBO
    FBS --> VISUAL
    FBO --> VISUAL
    TASKS --> FBS
    TASKS --> FBO
    TASKS --> EARNINGS
    PACKING --> FBO
    ASSEMBLY --> FBS
    ASSEMBLY --> FBO
    MOVEMENTS --> FBS
    MOVEMENTS --> FBO
    STAFF --> TASKS
    STAFF --> EARNINGS
    MOYSKLAD --> CARDS
    MOYSKLAD --> MATERIALS
    OZON_INT --> BARCODES
    WB_INT --> BARCODES
    TASKS --> REPORTS
    MOVEMENTS --> DASHBOARD

    style PRODUCTS fill:#dbeafe,stroke:#2563eb
    style WAREHOUSE fill:#d1fae5,stroke:#059669
    style OPERATIONS fill:#fef3c7,stroke:#d97706
    style PEOPLE fill:#f5f3ff,stroke:#7c3aed
    style ANALYTICS fill:#fee2e2,stroke:#dc2626
    style INTEGRATIONS fill:#fce7f3,stroke:#db2777
```

## Пользовательские роли

```mermaid
graph TD
    subgraph ROLES["Роли и доступ"]
        ADMIN_ROLE["🔑 Admin<br/>Полный доступ"]
        MANAGER_ROLE["📋 Manager<br/>Просмотр + склады + задачи"]
        EMPLOYEE_ROLE["👷 Employee<br/>Свои задачи + сканирование"]
    end

    ADMIN_ROLE --> D["Дашборд"]
    ADMIN_ROLE --> P["Товары / Сырьё"]
    ADMIN_ROLE --> W["Склады"]
    ADMIN_ROLE --> T["Задачи"]
    ADMIN_ROLE --> A["Аналитика"]
    ADMIN_ROLE --> E["Заработок"]
    ADMIN_ROLE --> M["Перемещения"]
    ADMIN_ROLE --> S["Сотрудники"]
    ADMIN_ROLE --> SET["Настройки"]

    MANAGER_ROLE --> D
    MANAGER_ROLE --> W
    MANAGER_ROLE --> T
    MANAGER_ROLE --> M
    MANAGER_ROLE --> A

    EMPLOYEE_ROLE --> MY_TASKS["Мои задачи"]
    EMPLOYEE_ROLE --> SCAN["Сканирование"]
    EMPLOYEE_ROLE --> MOVE["Перемещения"]
    EMPLOYEE_ROLE --> INV["Мой инвентарь"]

    style ADMIN_ROLE fill:#7c3aed,color:#fff
    style MANAGER_ROLE fill:#2563eb,color:#fff
    style EMPLOYEE_ROLE fill:#059669,color:#fff
```

## Связи

- [[Общая схема]] — стек технологий
- [[База данных]] — ER-диаграмма
- [[API эндпоинты]] — все роуты
- [[Деплой]] — CI/CD pipeline
